describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { VgmConverterCoreModule } from './core.module';

describe('VgmConverterCoreModule', () => {
  it('should work', () => {
    expect(new VgmConverterCoreModule(null)).toBeDefined();
  });
});
*/
